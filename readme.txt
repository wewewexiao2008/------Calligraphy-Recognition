使用说明：输入run_test然后打开需要测试的图（要选‘所有文件’）
然后输入3个参数，
需要展示的匹配字个数num（不要大于35，绘图区域未设置太大）
检索范围threshold，基于匹配值最高的字在其周围搜索（一般设置为跟num一样大），因为我数据集提取出来是有序的。
尝试次数try times，规定最大从匹配值序列中选取前多少个点进行检索范围判断。

内置了数据集的属性矩阵 Processed.mat
若要重新生成，则输入data_read，选择cao或者kai，（‘**未知’暂未测试能否运行）然后选择‘数据集’。